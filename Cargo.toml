[package]
name = "ifbridge"
version = "0.1.1"
edition = "2024"
description = "macOS-only typed access to bridge(4) forwarding database via private ioctls"
license = "MIT OR Apache-2.0"
repository = "https://github.com/arcboxlabs/ifbridge"

[dependencies]
libc = "0.2"
bitflags = "2"
